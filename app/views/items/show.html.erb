 <h1><%= @item.title %></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @item.body %></p>
   </div>
 <div class="col-md-4">
   <%= link_to "Edit", edit_item_path(@item), class: 'btn btn-success' %>
   <%= link_to "Delete Post", @item, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this Wiki?'} %>
   </div>
 </div>
s
